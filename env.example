# Banco de dados
DATABASE_URL=postgresql://residuum:residuum@localhost:5432/residuum

# Postgres (usado pelo docker-compose)
POSTGRES_USER=residuum
POSTGRES_PASSWORD=residuum
POSTGRES_DB=residuum
POSTGRES_PORT=5432

# JWT
SECRET_KEY=troque-esta-chave-em-producao
